name = "studfy-ai"
main = "worker.js"
compatibility_date = "2024-11-01"

# Anahtar koda YAZILMAZ. Gizli olarak ayarla:
#   wrangler secret put GEMINI_API_KEY
# veya Cloudflare panelinde Settings > Variables > Add (Encrypt).
